'use client'

// ScrollProgressIndicator is intentionally disabled —
// scroll progress is shown via the camera HUD values in HeroSection.
export default function ScrollProgressIndicator() {
  return null
}
